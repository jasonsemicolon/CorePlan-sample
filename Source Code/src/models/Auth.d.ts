export type User = {
  userName: string;
  email: string;
};

export type NewUser = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};

export type LoginType = {
  userName: string;
  password: string;
};
